Start the ioc server with the command:
iocsh -r ioc-linde-freia[,version] -c "requireSnippet(linde-freia.cmd, "PLC_IPADDR=<ip_address>")"

To start it automatically on diskless target write a following in /opt/startup/ioc/<target_name>/{ioc_name}/st.cmd:

require ioc-linde-freia[,version]
requireSnippet(linde-freia.cmd, "PLC_IPADDR=<ip_address>")
